<?xml version="1.0" encoding="utf-8" ?>
<!-- 
  NOTE: This application uses a custom SimpleLogger instead of NLog.
  This file is kept for reference only and is not actively used.
  
  Logging is handled by: Utilities\SimpleLogger.cs
  Log format: [MACHINE_IDENTIFIER][TIMESTAMP][LEVEL][PID][LOCATION] - MESSAGE
  Log location: logs\[YYYY-MM-DD].log
-->
<configuration>
  <!-- NLog configuration not in use - using SimpleLogger instead -->
</configuration>
